baidu_r:
  name: baidu guize
  page: baidu
  setting: baidu_setting
  #     mainSelector: string
  #    innerSelector: string
  #    condition: Condition
  mainSelector: a
  # innerSelector:
  condition:
    innerHTML: true
    